name: Tests

on: [push, pull_request]

jobs:
  test:
    runs-on: macos-latest
    
    steps:
    - uses: actions/checkout@v7
    - uses: swift-actions/setup-swift@v3
      with:
        swift-version: '6.0'
    - run: swift test